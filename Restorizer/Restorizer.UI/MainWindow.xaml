<Window x:Class="Restorizer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restorizer.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="1" Grid.Row="0" Background="#ffd6e3" HorizontalContentAlignment="Center"
               FontSize="30" Foreground="BLack" FontFamily="Verdana" Padding="60 20" VerticalContentAlignment="Center">

            Hello
            
        </Label>

        <Frame x:Name="MainFrame" Grid.Column="1" Grid.Row="1">
            
        </Frame>

        <Label Grid.Column="0" Grid.Row="0" Background="#ffb6cc" FontSize="30" Padding="30 20" Foreground="Black" FontFamily="Verdana">
            
            Restorizer
            
        </Label>

        <Grid Grid.Column="0" Grid.Row="1" Background="#ffc5d7" >

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Margin="10 20 10 10" Padding="10">
                DashBoard
            </Button>
            <Button Grid.Row="1" Margin="10" Padding="10">
                Dishes
            </Button>
            <Button Grid.Row="2" Margin="10" Padding="10">
                Orders
            </Button>
            <Button Grid.Row="3" Margin="10" Padding="10">
                Supplies
            </Button>
            
        </Grid>
        
    </Grid>
</Window>
